replicaCount: 1

image:
  repository: ghcr.io/sirajmoideen/uptime-monitor
  tag: latest
  pullPolicy: Always

imagePullSecrets:
  - name: ghcr-secret

containerPort: 5000

service:
  port: 80

ingress:
  enabled: true
  host: uptime.local

secretName: uptime-monitor-secret